# Service Hub Sample Repo

This sample repository is intentionally different from `sample_repo`.

Characteristics:
- Multi-module service layout (`service/`, `workers/`)
- API-style startup file (`service/app.py`)
- Operations runner (`workers/scheduler.py`)
- Environment-driven configuration (`config/settings.py`)

Use this repo in the MCP demo to prove repository switching works.
